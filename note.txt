install font awesome 
- npm install @fortawesome/fontawesome-free
- in the file /resources/css/app.css: @import "@fortawesome/fontawesome-free/css/all.css";
- run -> npm run build or npm run dev

prevent back history
- php artisan make:middleware PreventBackHistory

  public function handle($request, Closure $next)
    {
        $response = $next($request);
        return $response->header('Cache-Control','nocache, no-store, max-age=0, must-revalidate')
            ->header('Pragma','no-cache')
            ->header('Expires','Sun, 02 Jan 1990 00:00:00 GMT');
    }

- app/Http/Kernel.php

  'prevent-back-history' => \App\Http\Middleware\PreventBackHistory::class,

request update
- hapus dark mode
- mobile version (update data customer)
- title page disesuaikan
- cek file layouts ada yg perlu dihapus kayanya
- tombol upload desain tambah loading
- setiap ajax tambah error handling
- ulasan di produk detail di batasi
